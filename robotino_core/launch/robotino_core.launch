<launch>

  <!-- Arguments -->
  <arg name="robot_id" default="15" />

  <!-- Core node -->
  <node pkg="robotino_core" type="robotino_core_node" name="robotino_core_node">
    <param name="robot_id" value="$(arg robot_id)" />
  </node>
  
</launch>
